import React from 'react';
import { render } from 'react-dom';
import StartStepper from './containers/StartStepper';

const rootElement = document.getElementById('root')!;
render(<StartStepper/>, rootElement);
